{
  "name": "@streamzio/db",
  "version": "0.0.1",
  "main": "build/index.js",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "rimraf build && tsc",
    "generate": "prisma generate",
    "push": "prisma db push --skip-generate"
  },
  "devDependencies": {
    "prisma": "^5.8.1",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "@prisma/client": "^5.8.1"
  }
}
